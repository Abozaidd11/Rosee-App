import { OpenAddressesModalButton } from "./_components/open-addresses-modal-button";

export default function checkoutPage() {
  return (
    <main className="dark:bg-zinc-800 h-screen flex items-center justify-center">
      <OpenAddressesModalButton />
    </main>
  );
}
